#!/bin/bash

set -e

indent() {
  sed -u 's/^/       /'
}

echo "-----> Install heroku-buildpack-forge"

BUILD_DIR="$1"
ENV_DIR="$3"

./steps/forge-pro-private-key "$ENV_DIR"
./steps/managepy "$BUILD_DIR"
