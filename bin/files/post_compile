#!/bin/bash -e
npm run build || echo "Failed to run \`npm run build\` in post_compile"

# Run the Python buildpack collecstatic line again...
# (variables should be available in this context)
sub_env "$BIN_DIR/steps/collectstatic"
